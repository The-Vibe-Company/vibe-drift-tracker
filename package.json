{
  "name": "vibedrift-tracker",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "setup": "pnpm build && echo 'Run: pnpm vibedrift init --api-url <URL>'"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.15.0",
  "version": "0.1.2"
}
